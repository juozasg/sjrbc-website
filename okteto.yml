name: supervisord
command: supervisord -n
workdir: /okteto
sync:
- .:/okteto
- www:/var/www/html
forward:
- 8080:80
image:
  name: okteto.dev/supervisord-nginx:latest
  context: .
  dockerfile: Dockerfile
